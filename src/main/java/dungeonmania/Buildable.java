package dungeonmania;

/**
 * An interface for buildable entities (Complex and useful entities that are built by Player using a "recipe"). Has following properties:
 *      - Once Player constructs a buildable entity it is stored in the inventory and consequently all entities contained in the recipe 
 *          will be destroyed and thus will removed from inventory.
 */
public interface Buildable {
    
}
